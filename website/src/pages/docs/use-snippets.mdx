# Use snippets

After creating some snippets, you can use them in any input box.

Just type `/`, the snippets panel will show up.

You can select a snippet by typing prefix, using arrow keys or mouse, then press `Enter` to insert it.

💡 Tips: The trigger symbol which are set by default as `/`, can be customized in the settings.
